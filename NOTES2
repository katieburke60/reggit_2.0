
<div className="container">
  <div className="col-md-9 pull-right">
    {regulations}
  </div>
</div>

<div id="sidebar" className="sidebar" >
  <div className="col-md-2">
    <button
      className="btn-main btn-outline-default btn-space"
      onClick={this.handleAllClicked}>
      All Regs
    </button>
    <p>{categories}</p>
  </div>
</div>
);
}
}


// dispatch({type: "FILTER_REGULATIONS", filter: "all"})

// action = {
//   type: "FILTER_REGULATIONS_CATEGORY",
//   filter: {
//     category: "transportation",
//     categories: ["transportation", "education"]
//   }
// }

    // case "FILTER_REGULATIONS_BY_CATEGORY":
    //   return Object.assign({}, state, {
    //     filtered: state.all.filter((regulation) => regulation.category === action.filter.category )
    //   })
    // case "FILTER_REGULATIONS_BY_CATEGORIES":
    //   let regulations = []
    //   action.filter.categories.forEach((cat) => {
    //     regulations.concat(state.all.filter((regulation) => regulation.category === cat ))
    //   })
    //   return Object.assign({}, state, {
    //     filtered: regulations
    //   })
    // case "FILTER_REGULATIONS_BY_CATEGORIES":
    //   return Object.assign({}, state, {
    //     filtered: regulations
    //   })
    // case "FILTER_REGULATIONS_BY_CATEGORIES":
    //   return Object.assign({}, state, {
    //     filtered: regulations
    //   })
